import { PairsScreen } from "@/features/pairs/components/pairs-screen";

export const metadata = { title: "Reps & pairs — FieldPulse" };

export default function PairsPage() {
  return <PairsScreen />;
}
