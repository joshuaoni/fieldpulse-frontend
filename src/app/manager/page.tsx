import { TeamVisitsScreen } from "@/features/visits/components/team-visits-screen";

export const metadata = { title: "Check-ins — FieldPulse" };

export default function ManagerPage() {
  return <TeamVisitsScreen />;
}
