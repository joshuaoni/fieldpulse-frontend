import { ActivityScreen } from "@/features/activity/components/activity-screen";

export const metadata = { title: "Activity log — FieldPulse" };

export default function ActivityPage() {
  return <ActivityScreen />;
}
