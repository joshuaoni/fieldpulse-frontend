import { ReportsScreen } from "@/features/reports/components/reports-screen";

export const metadata = { title: "Reports — FieldPulse" };

export default function ReportsPage() {
  return <ReportsScreen />;
}
